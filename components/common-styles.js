import {StyleSheet} from 'react-native';

const listStyles = StyleSheet.create({
    floatingIconButton: {
        position: 'absolute',
        bottom: 10,
        right: 10,
        color: '#007AFF'
    }
});

export default listStyles;